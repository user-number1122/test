import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Импортируем основной компонент App
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root') // Точка входа в приложение
);
